// (1) edit style src & the file
import './kelaltech.com/styles/index.scss'

// (2) edit config src & the file
import config from './kelaltech.com/kelaltech.com'

// (3) edit /data/theme-src.scss

// (4) edit /.env

// (5) edit /public/manifest.json and the images in that /public/ folder

export default config
